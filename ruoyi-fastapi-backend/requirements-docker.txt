#===========================================================
# Docker 构建专用依赖（短剧平台）
# 用途：docker build 时使用，不含 AI module 依赖
#===========================================================

# Core framework
fastapi==0.128.2
uvicorn[standard]==0.34.0
pydantic==2.11.4
pydantic-settings==2.8.1
pydantic-validation-decorator==0.1.5
python-multipart==0.0.20
aiofiles==25.1.0

# Database
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
pymysql==1.1.1
greenlet==3.1.1

# Redis
redis==7.1.0

# Utils
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dateutil==2.9.0
email-validator==2.2.0
gevent==24.11.1

# Volcano Engine TOS
tos==2.8.3

# Data processing (admin feature)
pandas==2.3.3
openpyxl==3.1.5

# Image processing (admin upload)
Pillow==12.1.1

# System info (admin dashboard)
psutil==7.2.2

# HTTP client (tos service uses requests internally)
httpx==0.28.1

# Alembic migrations
alembic==1.14.0
